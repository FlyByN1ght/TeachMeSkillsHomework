package NinthHW;

public class Worker implements IPositionsable {

    // Реализация метода из интерфейса, возвращающего название должности
    @Override
    public String nameOfPositions() {
        return "I'm Worker";
    }
}
